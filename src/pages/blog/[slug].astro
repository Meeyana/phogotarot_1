---
// 1. Import Layout
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// 2. Code logic cũ (Giữ nguyên)
export async function getStaticPaths() {
  const allPosts = await getCollection('blog');
  return allPosts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}
const { post } = Astro.props;
const { Content } = await post.render();

// 3. (MỚI) LẤY TẤT CẢ BÀI VIẾT
const allPosts = await getCollection('blog');

// (MỚI) Xử lý ngày đăng (Phần này đã an toàn)
const postDate = post.data.pubDate ? new Date(post.data.pubDate) : null;
const displayDate = postDate ? postDate.toLocaleDateString('vi-VN', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
}) : null;

// (MỚI) Lọc bài viết liên quan
const relatedPosts = allPosts
  .filter(p => p.slug !== post.slug) // Lọc bài hiện tại
  .slice(0, 3); // Lấy 3 bài (logic đơn giản)

// 4. (CẬP NHẬT) TẠO SCHEMA JSON-LD
const schema = {
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": post.data.title,
  "description": post.data.excerpt,
  "image": new URL(post.data.image, Astro.site).href,
  "author": {
    "@type": "Organization",
    "name": "Phở Gõ Tarot"
  },
  "publisher": {
    "@type": "Organization",
    "name": "Phở Gõ Tarot",
    "logo": {
      "@type": "ImageObject",
      "url": "https://phogotarot.com/images/logo.png"
    }
  },
  // Thêm ngày đăng nếu có
  ...(postDate && { "datePublished": postDate.toISOString() })
};

// (MỚI) Lấy URL hiện tại để chia sẻ
const currentUrl = Astro.url.href;
const shareImage = new URL(post.data.image, Astro.site).href;
---

<BaseLayout
  title={`${post.data.title} - Phở Gõ Tarot`}
  description={post.data.excerpt}
  ogImage={post.data.image}
>
  <main id="homepage-content">
    <section class="homepage-section" style="padding-top: 4rem; min-height: 80vh;">
      <div class="container">
        
        <nav class="breadcrumbs">
          <a href="/">Trang chủ</a>
          <span class="divider">&raquo;</span>
          <a href="/blog">Blog</a>
          <span class="divider">&raquo;</span>
          <span class="current">{post.data.title}</span>
        </nav>

        <article class="post-main-content">
          <h1 class="hero-title">{post.data.title}</h1>

          {displayDate && (
            <div class="post-meta">
              <span>Ngày đăng: </span>
              <time datetime={postDate.toISOString()}>{displayDate}</time>
            </div>
          )}

          <img src={post.data.image} alt={post.data.title} class="section-image" />

          <div id="post-content">
            <Content />
          </div>

          <div class="share-buttons">
            <span>Chia sẻ:</span>
            <a 
              href={`https://www.facebook.com/sharer/sharer.php?u=${currentUrl}`} 
              class="share-btn facebook" 
              target="_blank" 
              rel="noopener noreferrer"
              aria-label="Chia sẻ lên Facebook"
            >
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M9 8h-3v4h3v12h5v-12h3.642l.358-4h-4v-1.667c0-.955.192-1.333 1.115-1.333h2.885v-5h-3.808c-3.596 0-5.192 1.583-5.192 4.615v2.385z"/></svg>
            </a>
            <a 
              href={`https://pinterest.com/pin/create/button/?url=${currentUrl}&media=${shareImage}&description=${post.data.title}`}
              class="share-btn pinterest" 
              target="_blank" 
              rel="noopener noreferrer"
              aria-label="Ghim lên Pinterest"
            >
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.627 0-12 5.373-12 12s5.373 12 12 12 12-5.373 12-12-5.373-12-12-12zm-0.091 19.34c-1.304 0-2.464-.812-2.812-2.006l-.994-3.842c-.22-.851.385-1.656 1.193-1.656.551 0 .807.412.807.91s-.312 1.337-.465 1.931c-.156.612.304 1.125.9 1.125.994 0 1.742-1.222 1.742-2.854 0-2.522-1.771-4.444-4.225-4.444-3.132 0-5.237 2.37-5.237 5.045 0 1.222.427 2.487 1.106 3.109.111.1.128.24.088.375l-.2 1.051c-.04.212-.174.253-.335.152-1.291-.812-2.062-2.37-2.062-4.148 0-3.616 2.91-6.762 7.029-6.762 3.73 0 6.364 2.656 6.364 6.138 0 3.744-2.152 6.333-5.223 6.333z"/></svg>
            </a>
          </div>

          <div class="cta-banner">
            <div class="cta-content">
              <h3>Bạn đang tìm kiếm câu trả lời?</h3>
              <p>Hãy để những lá bài Tarot soi sáng con đường của bạn. Trải nghiệm một phiên đọc bài cá nhân hoá ngay hôm nay.</p>
            </div>
            <a href="/boi-tarot-online" class="cta-button">Trải Bài Ngay</a>
          </div>

          <div class="related-posts">
            <h2 class="widget-title">Bài viết liên quan</h2>
            <div class="related-posts-grid">
              {relatedPosts.map(related => (
                <a href={`/blog/${related.slug}`} class="related-post-card">
                  <div class="related-card-img-wrapper">
                    <img src={related.data.image} alt={related.data.title} />
                  </div>
                  <h4>{related.data.title}</h4>
                  <p class="related-card-desc">{related.data.excerpt}</p>
                </a>
              ))}
            </div>
          </div>

          <a href="/blog" class="btn-outline" style="margin-top: 3rem;">&larr; Quay lại danh sách Blog</a>
        </article>

      </div>
    </section>
  </main>
    
  <script 
    type="application/ld+json" 
    slot="head"
    set:html={JSON.stringify(schema)}
  />

  <style>
    /* --- Cài đặt chung & Layout --- */
    .homepage-section { padding: 4rem 1rem; position: relative; z-index: 1; }
    
    .container { 
      max-width: 1000px; 
      margin: auto; 
      padding: 0 1rem; 
    }

    .post-main-content {
      width: 100%; 
    }

    .hero-title { 
      font-family: var(--font-title); 
      color: var(--title-color); 
      font-size: 3rem; 
      margin-bottom: 1rem; 
      text-align: left; 
      line-height: 1.2;
    }
    
    /* (SỬA) Ảnh chính: tăng max-height để rộng hơn */
    .section-image { 
      width: 100%; 
      height: auto; 
      max-height: 600px; /* Tăng từ 550px lên 600px */
      border-radius: 12px; 
      object-fit: cover; 
      margin-bottom: 2.5rem; 
    }
    
    .btn-outline { 
      display: inline-block; background: transparent; 
      border: 1px solid var(--card-back-border); 
      color: var(--title-color); padding: 0.75rem 1.5rem; 
      border-radius: 8px; font-weight: 600; 
      text-decoration: none; text-shadow: none; 
    }

    /* --- Breadcrumbs --- */
    .breadcrumbs {
      margin-bottom: 2rem;
      font-size: 0.9rem;
      opacity: 0.8;
      text-shadow: none;
    }
    .breadcrumbs a {
      color: var(--text-color);
      text-decoration: none;
    }
    .breadcrumbs a:hover {
      color: var(--title-color);
      text-decoration: underline;
    }
    .breadcrumbs .divider {
      margin: 0 0.5rem;
    }
    .breadcrumbs .current {
      font-weight: 600;
      color: var(--title-color);
    }

    /* --- Meta Ngày đăng --- */
    .post-meta {
      font-size: 0.9rem;
      opacity: 0.8;
      margin-bottom: 1.5rem;
      text-shadow: none;
    }
    .post-meta time {
      font-weight: 600;
      color: var(--title-color);
    }

    /* --- CSS Nội dung bài viết --- */
    #post-content {
      font-size: 1.1rem; line-height: 1.8; text-shadow: none;
      text-align: left;
    }
    #post-content h2, #post-content h3 {
      font-family: var(--font-title); color: var(--title-color);
      margin-top: 2.5rem; margin-bottom: 1rem;
    }
    #post-content p { margin-bottom: 1.5rem; }
    #post-content ul, #post-content ol { padding-left: 2rem; margin-bottom: 1.5rem; }
    
    /* (SỬA) Hình ảnh trong bài viết: giảm max-width đáng kể */
    #post-content :global(img) {
      display: block; 
      width: 100%;
      max-width: 600px; /* Giảm từ 750px xuống 600px */
      height: auto;
      border-radius: 8px;
      margin: 2rem auto; 
      border: 1px solid var(--card-back-border);
    }

    /* --- Nút chia sẻ Icon --- */
    .share-buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      align-items: center;
      margin: 3rem 0;
      padding: 1.5rem 0;
      border-top: 1px solid var(--card-back-border);
      border-bottom: 1px solid var(--card-back-border);
    }
    .share-buttons span {
      font-weight: 600;
      font-family: var(--font-title);
      font-size: 1.1rem;
      color: var(--title-color);
      margin-right: 0.5rem;
    }
    .share-btn {
      display: flex; 
      align-items: center; 
      justify-content: center; 
      width: 40px; 
      height: 40px; 
      border-radius: 50%; 
      text-decoration: none;
      text-shadow: none;
      color: #fff;
      transition: opacity 0.3s;
    }
    .share-btn:hover { opacity: 0.8; }
    .share-btn.facebook { background-color: #1877F2; }
    .share-btn.pinterest { background-color: #E60023; }

    /* --- CTA Banner --- */
    .cta-banner {
      background-image: linear-gradient(90deg, rgba(20, 40, 80, 0.9) 0%, rgba(40, 60, 100, 0.7) 100%);
      border: 1px solid var(--card-back-border);
      border-radius: 12px;
      padding: 2rem;
      display: flex;
      flex-direction: column; 
      justify-content: space-between;
      align-items: center;
      gap: 1.5rem;
      margin-bottom: 3rem;
      text-align: center; 
    }
    .cta-content h3 {
      font-family: var(--font-title);
      color: var(--title-color);
      font-size: 1.75rem;
      margin: 0 0 0.75rem 0;
    }
    .cta-content p {
      margin: 0;
      font-size: 1rem;
      opacity: 0.9;
      text-shadow: none;
      max-width: 500px;
    }
    .cta-button {
      background-color: var(--title-color);
      color: var(--bg-color);
      text-decoration: none;
      font-weight: 700;
      padding: 0.8rem 2rem;
      border-radius: 8px;
      text-shadow: none;
      transition: background-color 0.3s;
      flex-shrink: 0; 
    }
    .cta-button:hover {
      background-color: #b8d4ff;
    }
    @media (min-width: 768px) {
      .cta-banner {
        flex-direction: row;
        text-align: left;
      }
      .cta-content {
        flex-grow: 1;
      }
    }


    /* --- Bài viết liên quan --- */
    .widget-title { 
      font-family: var(--font-title);
      color: var(--title-color);
      margin-top: 0;
      margin-bottom: 1.5rem;
      font-size: 1.5rem;
    }
    .related-posts {
      margin-bottom: 2rem;
    }
    .related-posts-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
    }
    .related-post-card {
      display: flex; 
      flex-direction: column; 
      text-decoration: none;
      background-color: var(--button-bg);
      border: 1px solid var(--card-back-border);
      border-radius: 8px;
      overflow: hidden;
      transition: border-color 0.3s;
    }

    .related-card-img-wrapper {
      width: 100%;
      aspect-ratio: 16 / 10;
      overflow: hidden;
    }
    .related-post-card img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.3s;
    }
    .related-post-card:hover img {
      transform: scale(1.05);
    }
    .related-post-card h4 {
      font-family: var(--font-title);
      color: var(--text-color);
      font-size: 1.1rem; 
      line-height: 1.4;
      padding: 1rem 1rem 0.5rem 1rem; 
      margin: 0;
      text-shadow: none;
    }
    .related-card-desc {
      font-size: 0.9rem;
      opacity: 0.8;
      text-shadow: none;
      padding: 0 1rem 1rem 1rem;
      margin: 0;
      flex-grow: 1; 
      color:#fff;
    }

    /* --- Responsive cho Mobile --- */
    @media (max-width: 900px) {
      .hero-title {
        font-size: 2.5rem;
      }
    }
    
    @media (max-width: 768px) { 
      .related-posts-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 600px) {
      .homepage-section { padding: 4rem 0.5rem; }
      .container { padding: 0 0.5rem; }
      
      .related-posts-grid {
        grid-template-columns: 1fr;
      }
      .hero-title {
        font-size: 2rem;
      }
      #post-content {
        font-size: 1rem;
      }
      /* (MỚI) Giảm max-width ảnh trong bài viết */
      #post-content :global(img) {
        max-width: 100%;
      }
    }
  </style>
</BaseLayout>