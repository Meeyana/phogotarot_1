---
// 1. Import Layout
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// 2. (ĐÃ XÓA) Bỏ getStaticPaths()
// export async function getStaticPaths() { ... }

// 3. (MỚI) Lấy 'slug' từ URL của người dùng
const { slug } = Astro.params;

// 4. (MỚI) Lấy TẤT CẢ bài viết để tìm bài viết hiện tại
const allPosts = await getCollection('blog');

// 5. (MỚI) Tìm bài viết hiện tại dựa trên slug
const post = allPosts.find(p => p.slug === slug);

// 6. (MỚI) Xử lý 404 nếu không tìm thấy bài viết
if (!post) {
  return Astro.redirect('/404');
}

// 7. (GIỮ NGUYÊN) Render và lấy dữ liệu
const { Content } = await post.render();

// 8. (GIỮ NGUYÊN) Xử lý ngày đăng
const postDate = post.data.pubDate ? new Date(post.data.pubDate) : null;
const displayDate = postDate ? postDate.toLocaleDateString('vi-VN', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
}) : null;

// 9. (CẬP NHẬT) Lọc bài viết liên quan thông minh theo 'category'

// Lấy category của bài viết hiện tại
const currentCategory = post.data.category;
let relatedPosts = [];

// Kiểm tra xem bài này có category không
if (currentCategory) {
  // Lọc Lần 1: Lấy các bài viết CÓ CÙNG CATEGORY
  const postsInSameCategory = allPosts
    .filter(p => {
      // Lọc bỏ chính bài viết hiện tại VÀ
      // Lọc các bài có cùng category
      return p.slug !== post.slug && p.data.category === currentCategory;
    })
    // Sắp xếp các bài cùng category theo ngày mới nhất
    .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());

  
  if (postsInSameCategory.length >= 3) {
    // 1. Nếu có đủ 3 (hoặc nhiều hơn) bài cùng chủ đề -> Lấy 3 bài
    relatedPosts = postsInSameCategory.slice(0, 3);
  } else {
    // 2. Nếu không đủ 3 bài (ví dụ: chỉ có 1 bài cùng chủ đề)
    // Lấy hết các bài cùng chủ đề đó (ví dụ: lấy 1 bài đó)
    relatedPosts = [...postsInSameCategory];
  }
}

// 3. LOGIC LẤP ĐẦY (FILLER)
// Nếu sau khi lọc category mà vẫn chưa đủ 3 bài
if (relatedPosts.length < 3) {
  // Tạo một Set (tập hợp) các slug đã có để tránh trùng lặp
  const existingSlugs = new Set(relatedPosts.map(p => p.slug));
  existingSlugs.add(post.slug); // Thêm cả slug của bài hiện tại

  // Lọc Lần 2: Lấy thêm các bài MỚI NHẤT (trừ những bài đã có)
  const fillerPosts = allPosts
    .filter(p => !existingSlugs.has(p.slug)) // Lọc những bài đã có
    .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());

  // Tính xem cần lấp đầy bao nhiêu bài nữa
  const needed = 3 - relatedPosts.length;
  
  // Thêm các bài "filler" này vào
  relatedPosts.push(...fillerPosts.slice(0, needed));
}

// 10. (GIỮ NGUYÊN) TẠO SCHEMA JSON-LD
const schema = {
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": post.data.title,
  "description": post.data.excerpt,
  "image": new URL(post.data.image, Astro.site).href,
  "author": {
    "@type": "Organization",
    "name": "Phở Gõ Tarot"
  },
  "publisher": {
    "@type": "Organization",
    "name": "Phở Gõ Tarot",
    "logo": {
      "@type": "ImageObject",
      "url": "https://phogotarot.com/images/logo.png"
    }
  },
  ...(postDate && { "datePublished": postDate.toISOString() })
};

// 11. (GIỮ NGUYÊN) Lấy URL hiện tại để chia sẻ
const currentUrl = Astro.url.href;
const shareImage = new URL(post.data.image, Astro.site).href;
---

{/* ========================================================== */}
{/* TOÀN BỘ PHẦN HTML VÀ CSS BÊN DƯỚI ĐƯỢC GIỮ NGUYÊN Y HỆT     */}
{/* ========================================================== */}

<BaseLayout
  title={`${post.data.title} - Phở Gõ Tarot`}
  description={post.data.excerpt}
  ogImage={post.data.image}
>
  <main id="homepage-content">
    <section class="homepage-section" style="padding-top: 4rem; min-height: 80vh;">
      <div class="container">
        
        <nav class="breadcrumbs">
          <a href="/">Trang chủ</a>
          <span class="divider">&raquo;</span>
          {/* (SỬA) Sửa link breadcrumb về trang bài viết */}
          <a href="/bai-viet">Kiến Thức</a> 
          <span class="divider">&raquo;</span>
          <span class="current">{post.data.title}</span>
        </nav>

        <article class="post-main-content">
          <h1 class="hero-title">{post.data.title}</h1>

          {displayDate && (
            <div class="post-meta">
              <span>Ngày đăng: </span>
              <time datetime={postDate.toISOString()}>{displayDate}</time>
            </div>
          )}

          <img src={post.data.image} alt={post.data.title} class="section-image" />

          <div id="post-content">
            <Content />
          </div>

          <div class="share-buttons">
            <span>Chia sẻ:</span>
            <a 
              href={`https://www.facebook.com/sharer/sharer.php?u=${currentUrl}`} 
              class="share-btn facebook" 
              target="_blank" 
              rel="noopener noreferrer"
              aria-label="Chia sẻ lên Facebook"
            >
              <i class="fa-brands fa-facebook-f"></i>
            </a>
            <a 
              href={`https://pinterest.com/pin/create/button/?url=${currentUrl}&media=${shareImage}&description=${post.data.title}`}
              class="share-btn pinterest" 
              target="_blank" 
              rel="noopener noreferrer"
              aria-label="Ghim lên Pinterest"
            >
              <i class="fa-brands fa-pinterest"></i>
            </a>
          </div>

          <div class="cta-banner">
            <div class="cta-content">
              <h3>Bạn đang tìm kiếm câu trả lời?</h3>
              <p>Hãy để những lá bài Tarot soi sáng con đường của bạn. Trải nghiệm một phiên đọc bài cá nhân hoá ngay hôm nay.</p>
            </div>
            <a href="/tarot" class="cta-button">Trải Bài Ngay</a>
          </div>

          <div class="related-posts">
            <h2 class="widget-title">Bài viết liên quan</h2>
            <div class="related-posts-grid">
              {relatedPosts.map(related => (
                <a href={`/blog/${related.slug}`} class="related-post-card">
                  <div class="related-card-img-wrapper">
                    <img src={related.data.image} alt={related.data.title} />
                  </div>
                  <h4>{related.data.title}</h4>
                  <p class="related-card-desc">{related.data.excerpt}</p>
                </a>
              ))}
            </div>
          </div>

          {/* (SỬA) Sửa link nút quay lại */}
          <a href="/bai-viet" class="btn-outline" style="margin-top: 3rem;">&larr; Quay lại danh sách</a>
        </article>

      </div>
    </section>
  </main>
    
  <script 
    type="application/ld+json" 
    slot="head"
    set:html={JSON.stringify(schema)}
  />

  <style>
    /* --- Cài đặt chung & Layout --- */
    .homepage-section { padding: 4rem 1rem; position: relative; z-index: 1; }
    .container { 
      max-width: 1000px;
      margin: auto; 
      padding: 0 1rem; 
    }
    .post-main-content {
      width: 100%;
    }
    .hero-title { 
      font-family: var(--font-title); 
      color: var(--title-color); 
      font-size: 3rem; 
      margin-bottom: 1rem;
      text-align: left; 
      line-height: 1.2;
    }
    .section-image { 
      width: 100%;
      height: auto; 
      max-height: 600px;
      border-radius: 12px; 
      object-fit: cover;
      margin-bottom: 2.5rem; 
    }
    .btn-outline { 
      display: inline-block;
      background: transparent; 
      border: 1px solid var(--card-back-border); 
      color: var(--title-color); padding: 0.75rem 1.5rem; 
      border-radius: 8px; font-weight: 600; 
      text-decoration: none; text-shadow: none;
    }

    /* --- Breadcrumbs --- */
    .breadcrumbs {
      margin-bottom: 2rem;
      font-size: 0.9rem;
      opacity: 0.8;
      text-shadow: none;
    }
    .breadcrumbs a {
      color: var(--text-color);
      text-decoration: none;
    }
    .breadcrumbs a:hover {
      color: var(--title-color);
      text-decoration: underline;
    }
    .breadcrumbs .divider {
      margin: 0 0.5rem;
    }
    .breadcrumbs .current {
      font-weight: 600;
      color: var(--title-color);
    }

    /* --- Meta Ngày đăng --- */
    .post-meta {
      font-size: 0.9rem;
      opacity: 0.8;
      margin-bottom: 1.5rem;
      text-shadow: none;
    }
    .post-meta time {
      font-weight: 600;
      color: var(--title-color);
    }

    /* --- CSS Nội dung bài viết --- */
    #post-content {
      font-size: 1.1rem;
      line-height: 1.8; text-shadow: none;
      text-align: left;
    }
    #post-content h2, #post-content h3 {
      font-family: var(--font-title);
      color: var(--title-color);
      margin-top: 2.5rem; margin-bottom: 1rem;
    }
    #post-content p { margin-bottom: 1.5rem; }
    #post-content ul, #post-content ol { padding-left: 2rem; margin-bottom: 1.5rem; }
    
    #post-content :global(img) {
      display: block;
      width: 100%;
      max-width: 600px;
      height: auto;
      border-radius: 8px;
      margin: 2rem auto; 
      border: 1px solid var(--card-back-border);
    }
    #post-content :global(a) {
      color: #009eff;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s, text-decoration 0.3s;
    }
    #post-content :global(a:hover) {
      color: orange;
      text-decoration: underline;
    }
    #post-content :global(.text-center) {
      text-align: center;
    }

    /* --- Nút chia sẻ Icon --- */
    .share-buttons {
      display: flex; flex-wrap: wrap;
      gap: 1rem;
      align-items: center;
      margin: 3rem 0;
      padding: 1.5rem 0;
      border-top: 1px solid var(--card-back-border);
      border-bottom: 1px solid var(--card-back-border);
    }
    .share-buttons span {
      font-weight: 600;
      font-family: var(--font-title);
      font-size: 1.1rem;
      color: var(--title-color);
      margin-right: 0.5rem;
    }
    .share-btn {
      display: flex; 
      align-items: center; 
      justify-content: center;
      width: 40px; 
      height: 40px; 
      border-radius: 50%; 
      text-decoration: none;
      text-shadow: none;
      color: #fff;
      transition: opacity 0.3s;
    }
    .share-btn:hover { opacity: 0.8; }
    .share-btn.facebook { background-color: #1877F2; }
    .share-btn.pinterest { background-color: #E60023; }

    /* --- CTA Banner --- */
    .cta-banner {
      background-image: linear-gradient(90deg, rgba(20, 40, 80, 0.9) 0%, rgba(40, 60, 100, 0.7) 100%);
      border: 1px solid var(--card-back-border);
      border-radius: 12px;
      padding: 2rem;
      display: flex;
      flex-direction: column; 
      justify-content: space-between;
      align-items: center;
      gap: 1.5rem;
      margin-bottom: 3rem;
      text-align: center; 
    }
    .cta-content h3 {
      font-family: var(--font-title);
      color: var(--title-color);
      font-size: 1.75rem;
      margin: 0 0 0.75rem 0;
    }
    .cta-content p {
      margin: 0;
      font-size: 1rem;
      opacity: 0.9;
      text-shadow: none;
      max-width: 500px;
    }
    .cta-button {
      background-color: var(--title-color);
      color: var(--bg-color);
      text-decoration: none;
      font-weight: 700;
      padding: 0.8rem 2rem;
      border-radius: 8px;
      text-shadow: none;
      transition: background-color 0.3s;
      flex-shrink: 0;
    }
    .cta-button:hover {
      background-color: #b8d4ff;
    }
    @media (min-width: 768px) {
      .cta-banner {
        flex-direction: row;
        text-align: left;
      }
      .cta-content {
        flex-grow: 1;
      }
    }

    /* --- Bài viết liên quan --- */
    .widget-title { 
      font-family: var(--font-title);
      color: var(--title-color);
      margin-top: 0;
      margin-bottom: 1.5rem;
      font-size: 1.5rem;
    }
    .related-posts {
      margin-bottom: 2rem;
    }
    .related-posts-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
    }
    .related-post-card {
      display: flex; 
      flex-direction: column; 
      text-decoration: none;
      background-color: var(--button-bg);
      border: 1px solid var(--card-back-border);
      border-radius: 8px;
      overflow: hidden;
      transition: border-color 0.3s;
    }
    .related-card-img-wrapper {
      width: 100%;
      aspect-ratio: 16 / 10;
      overflow: hidden;
    }
    .related-post-card img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.3s;
    }
    .related-post-card:hover img {
      transform: scale(1.05);
    }
    .related-post-card h4 {
      font-family: var(--font-title);
      color: var(--text-color);
      font-size: 1.1rem; 
      line-height: 1.4;
      padding: 1rem 1rem 0.5rem 1rem; 
      margin: 0;
      text-shadow: none;
    }
    .related-card-desc {
      font-size: 0.9rem;
      opacity: 0.8;
      text-shadow: none;
      padding: 0 1rem 1rem 1rem;
      margin: 0;
      flex-grow: 1; 
      color:#fff;
    }

    /* --- Responsive cho Mobile --- */
    @media (max-width: 900px) {
      .hero-title {
        font-size: 2.5rem;
      }
    }
    @media (max-width: 768px) { 
      .related-posts-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    @media (max-width: 600px) {
      .homepage-section { padding: 4rem 0.5rem; }
      .container { padding: 0 0.5rem; }
      .related-posts-grid {
        grid-template-columns: 1fr;
      }
      .hero-title {
        font-size: 2rem;
      }
      #post-content {
        font-size: 18px;
      }
      #post-content :global(img) {
        max-width: 100%;
      }
    }
  </style>
</BaseLayout>